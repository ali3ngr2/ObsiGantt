{
  "name": "task-gantt",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production"
  },
  "dependencies": {
    "frappe-gantt": "^0.6.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "obsidian": "latest",
    "tslib": "2.6.0",
    "typescript": "5.4.2"
  }
}
